include ../bullet-list/bullet-list

mixin rules()
  section.rules(class = attributes.class)
    h2.rules__title Правила
    +bullet-list(
      {
        names: [
          'Нельзя с питомцами',
          'Без вечеринок и мероприятий',
          'Время прибытия — после 13:00, а выезд до 12:00'
        ],
      }
    ).rules__bullet-list